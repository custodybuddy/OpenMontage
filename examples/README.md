# Examples

This directory contains concrete, non-canonical examples built on top of OpenMontage.

- `workflows/` contains product-specific workflow briefs.

Core pipeline behavior belongs in `pipeline_defs/` and `skills/pipelines/`. Generated
project artifacts and media belong in the gitignored `projects/` directory.
